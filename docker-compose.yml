services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - FF_LLM_MODE=mock
    volumes:
      - ff-data:/data

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - api

volumes:
  ff-data:
